# Angular-Laravel-Authentication-with-JWT
Angular 7 and Laravel 5.7 Authentication system using JWT

This Repo contains a simple authentication system, Angular as the frontend and laravel as the backend
